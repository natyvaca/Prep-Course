Variables: son elementos configurables que nos sirven para darle valor a un elemento, este valor se mantiene hasta que se le asigna un nuevo valor a dicho elemento, las variables nos sirven para poder utilizar un mismo elemnto con un valor particular, sin la necesidad de estar configurandolo cada vez que se utiliza.
Strings: son bloques de texto, puede tener números incluidos, es una cadena de caracteres que siempre se encierra entre comillas, citando de esta manera que todo lo incluido dentro de las comillas es un strings.
Funciones:  son agrupaciones de códigos que se utilizan para evitar escribir muchas veces el mismo código. estas se definen por medio del comando function, seguido del nombre de la misma más el símbolo llave dentro de la función se configuran las variables mediante el comando console.log y entre parentesis los valores de estas, al finalizar el ingreso de las variables se cierra la función nuevamente con llave este ingreso de variables y valores se llama argumento.Return es un comando que se utiliza para aclararle al interprete que la función termina ahí y debe devolverme el valor especificado en la palabra return y luego de esta palabra, ningún valor especificado se toma en cuenta.
Declaraciones: las declaraciones son valores que dependiendo del resultado nos van a llevar a continuar con la función con un resultado u otro, esto nos permite configurar el comando IF, por ejemplo si el perro es marrón va al grupo de los comunes, si el perro es azul va al grupo de los extraños, osea dependiendo del resultado del comando nos lleva a una opción u otra.
valores booleanos: estos derivan del codigo binario y en este lenguaje se usan con los comandos true o false, nos sirve para gregar lógica al código y de acuerdo al resultado nos lleva a una resolución u otra dentro de la misma función.  
